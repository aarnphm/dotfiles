#!/usr/bin/env bash

if [[ `command -v git` ]]; then
    git clone --depth=1 --recursive git@github.com:aarnphm/dotfiles.git && cd dotfiles
    ./install.pre
else
    echo "git hasn't installed on the system yet. Install git before procedding"
    exit 1
fi

